version: "2.17.2"
services:
  w-app-backend:
    image: w-app-backend
    container_name: w-app-backend
    build: .
    restart: "always"
    env_file: .env
    ports:
      - "5000:5000"
    networks:
      - mynetwork

networks:
  mynetwork:
    driver: bridge
